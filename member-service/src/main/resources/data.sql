-- 초기 관리자 및 사용자 데이터
-- 비밀번호는 BCrypt로 암호화되어야 하므로, 애플리케이션 시작 시 초기화하거나 
-- 이미 암호화된 값으로 직접 삽입해야 합니다.

-- BCrypt 암호화된 비밀번호 예시:
-- "admin123" -> $2a$10$...
-- "user123" -> $2a$10$...

-- 주의: 실제 환경에서는 더 안전한 방법으로 초기 데이터를 설정해야 합니다.
-- 여기서는 MemberService나 별도의 초기화 클래스를 통해 데이터를 생성하는 것을 권장합니다.

